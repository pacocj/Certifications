# Certifications

This repository contains scans of certificates, mock certificates, and other various documents showcasing my journey through different learning paths, bootcamps, and courses.

## Projetos

- `tetris.py` — jogo simples de Tetris feito com `pygame`. Para jogar, instale a dependência (`pip install pygame`) e execute `python tetris.py`.
